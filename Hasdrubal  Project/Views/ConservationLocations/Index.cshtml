@model IEnumerable<Hasdrubal__Project.Models.ConservationLocation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Lieu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlaceDepot)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModeStockage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Oeuvre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Lieu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlaceDepot)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModeStockage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Oeuvre.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
